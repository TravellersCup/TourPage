export default async(req)=>{const cookie=req.headers.get('cookie')||'';const m=cookie.match(/(?:^|;\s*)tt_admin=([^;]+)/);const secret=process.env.ADMIN_SESSION_SECRET;if(!m||!secret)return new Response('Unauthorized',{status:401});const enc=new TextEncoder();const key=await crypto.subtle.importKey('raw',enc.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);const sig=await crypto.subtle.sign('HMAC',key,enc.encode('travellers-admin-v1'));const expected=Buffer.from(sig).toString('base64url');return m[1]===expected?new Response(JSON.stringify({ok:true}),{headers:{'Content-Type':'application/json'}}):new Response('Unauthorized',{status:401})}
